'use client';

// FavoriteBar is now integrated into the Header component
// This component is kept for backward compatibility but always returns null
export default function FavoriteBar() {
  return null;
}
